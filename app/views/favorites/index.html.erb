<% if searcher? %>
  <h1>Your favorite dogs</h1>
  <% @favorites.each do |favorite| %>
      <p><%= link_to "#{favorite.dog.name}", dog_path(favorite.dog) %>
      <%= link_to '<i class="fa-solid fa-trash-can"></i>'.html_safe, dog_favorite_path(favorite.dog, favorite), data: {turbo_method: :delete, turbo_confirm: "Are you sure you want to delete #{favorite.dog.name} from your favorites?"} %></p>
      <%= link_to '<i class="fa-solid fa-message"></i>'.html_safe, favorite_chats_path(favorite), data: { turbo_method: :post }%>
  <% end %>
<%= link_to '<i class="fa-solid fa-arrow-left"></i> back to all dogs'.html_safe, dogs_path %>
<% end %>
